[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bun --preload ./src/env.ts src/index.ts"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
